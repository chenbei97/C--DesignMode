外观模式是一种程序设计思想，并不是一种特殊的编程手法

客户端尽可能的避免直接调用业务类，而是通过外观类提供的接口调用

这样业务类修改不会直接影响客户端的代码，只需要适当修改外观类的接口函数即可

每个业务类都可以有个外观类，一个业务类也可以有多个不同的外观类

1个例子：
    网络游戏有一些配置类，用于配置图像，声音和语音聊天等，这些可以使用单例模式
    如果客户端直接交互，会比较繁琐不好维护；此时可以使用1个外观类进行隔绝

外观模式没有特定的uml图，因为不是一种编程手法，uml.svg表示的示意图只是一个展示